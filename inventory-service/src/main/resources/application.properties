quarkus.http.host=0.0.0.0
quarkus.http.port=${INVENTORY_PORT:8083}
quarkus.smallrye-graphql.ui.always-include=true

# Seleção de repositório: memória é o default. Antes/na migração do ch.7,
# adicionar datasource (MySQL) e o adapter Panache, ativando o seletor nele.
app.repository=memory