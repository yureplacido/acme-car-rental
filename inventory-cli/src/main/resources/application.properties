quarkus.grpc.clients.inventory.host=localhost
quarkus.grpc.clients.inventory.port=9000
quarkus.grpc.server.port=-1

# Gera os stubs do cliente a partir do contrato org.acme:inventory-proto (fonte única).
quarkus.generate-code.grpc.scan-for-proto=org.acme:inventory-proto